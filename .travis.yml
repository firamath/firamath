sudo: required
services:
  - docker
install:
  - docker pull ubuntu
  - docker run ubuntu apt-get update && apt-get install --yes python-fontforge
script:
  - docker run ubuntu python ./scripts/build.py --fonts
